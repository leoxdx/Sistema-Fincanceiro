# Copie este arquivo para .env e ajuste os valores antes de rodar a aplicação
# Use este arquivo apenas como exemplo; nunca envie o arquivo .env real ao GitHub.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=SuaSenhaSecreta
